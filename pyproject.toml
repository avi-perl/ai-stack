[project]
name = "ai-stack"
version = "0.1.0"
description = "ai-stack monorepo root"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["apps/*"]
exclude = ["apps/ui"]
